---
import SectionHead from './SectionHead.astro';
import { whyUs } from '../site.config';
---

<section id="why" class="bg-bone-50 py-24 sm:py-28 lg:py-36">
  <div class="mx-auto max-w-7xl px-5 sm:px-8">
    <SectionHead
      eyebrow={whyUs.eyebrow}
      heading={whyUs.heading}
      intro={whyUs.intro}
      tone="light"
    />

    <ul
      class="mt-16 grid gap-px overflow-hidden rounded-2xl bg-steel-900/10 lg:mt-20 sm:grid-cols-2 lg:grid-cols-3"
    >
      {
        whyUs.items.map((item, i) => (
          <li
            data-reveal={i * 60}
            class="group bg-bone-50 p-8 transition-colors duration-300 hover:bg-white sm:p-10"
          >
            <span class="font-mono text-xs tracking-[0.2em] text-brand-500">
              {String(i + 1).padStart(2, '0')}
            </span>
            <h3 class="mt-5 font-display text-xl leading-tight font-extrabold tracking-tight text-steel-900 sm:text-2xl">
              {item.title}
            </h3>
            <p class="mt-3.5 text-[0.95rem] leading-relaxed text-steel-600">
              {item.body}
            </p>
            <span
              aria-hidden="true"
              class="mt-6 block h-0.5 w-10 bg-brand-500/25 transition-all duration-300 group-hover:w-20 group-hover:bg-signal-500"
            />
          </li>
        ))
      }
    </ul>
  </div>
</section>
